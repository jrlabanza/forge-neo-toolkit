# sd-forge-notify
"🔔 Notify when done" accordion at the bottom of txt2img/img2img. Fires a Windows toast and/or Discord webhook when a generation ran longer than the threshold. Enable it, set minimum seconds, Save settings. Test button included. Settings live in notify_settings.json here.

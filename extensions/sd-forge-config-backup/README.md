# sd-forge-config-backup
Snapshots config.json, ui-config.json and styles.csv to `_attic/config-autobackups/<timestamp>/` on every app start; keeps the newest 10. No UI. To restore: close Forge, copy files back from the newest good snapshot.
